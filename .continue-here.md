# Continue Here — BlockNow Deployment
**Paused:** 2026-02-25 ~3:00 AM

## What Was Done
- Cloned `Yahya-Naeem/BlockNow` repo (React 18 + Vite + Express full-stack app)
- Deployed to Railway (project ID: `509ff975-b901-4263-8b4e-debacdecb024`)
- Connected custom domain **www.blocknow.co.uk** via IONOS DNS (CNAME + TXT verification)
- Railway CLI authenticated as `umarmsharif@gmail.com`

### Code Fixes Applied
1. **Button arrow wrapping** — Added `w-fit` to NeonButton on hero and CTA, plus `inline-flex items-center whitespace-nowrap` to NeonButton inner span
2. **Dialog title clipping** — Added extra top padding to DemoRequestDialog
3. **Footer address spacing** — Fixed in `shared/schema.ts` ("Unit 8A, Midas Business Centre, Wantz Road, Dagenham, RM10 8PS, UK")
4. **Form submission hanging** — Replaced nodemailer (SMTP blocked by Railway IPv6) with Resend API (HTTPS-based, fire-and-forget)

### Files Modified
- `client/src/components/NeonButton.tsx` — whitespace-nowrap fix
- `client/src/components/DemoRequestDialog.tsx` — top padding for title
- `client/src/pages/Home.tsx` — w-fit on hero and final CTA buttons
- `server/routes.ts` — Replaced nodemailer with Resend API, fire-and-forget email
- `shared/schema.ts` — Address spacing fix
- `package.json` / `package-lock.json` — Added nodemailer (can be removed now since we switched to Resend)

## What's Still Pending

### 1. Email Notifications (HIGH PRIORITY)
- Form submissions currently log to console but don't email
- Need Resend API key: sign up at https://resend.com (free, 100 emails/day)
- Then set env var on Railway: `railway vars set RESEND_API_KEY=<key>`
- Emails will go to `umarmsharif@gmail.com`

### 2. Cleanup
- Remove `nodemailer` and `@types/nodemailer` from package.json (no longer used)
- Remove SMTP env vars from Railway (`SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`)

### 3. Root Domain Redirect
- Only `www.blocknow.co.uk` works; `blocknow.co.uk` (root) has no redirect
- IONOS doesn't support CNAME on root domain — would need an A record or IONOS redirect rule

## Environment
- **Railway project:** https://railway.com/project/509ff975-b901-4263-8b4e-debacdecb024
- **Live URL:** https://www.blocknow.co.uk
- **Railway URL:** https://blocknow-production.up.railway.app
- **Railway plan:** Trial (30 days / $4.99 left)
- **IONOS domain:** blocknow.co.uk (DNS managed at my.ionos.co.uk)

## How to Resume
```bash
cd /Users/foodpanda/projects/BlockNow
railway whoami  # verify auth
railway logs    # check current state
```
